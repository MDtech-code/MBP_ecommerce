// src/features/search/index.js
// Public API of the search feature.
// SearchBar lives here only if it manages state or calls API itself.
// If it is a dumb emitter it lives in shared/ui/SearchBar instead.

// ui
export { default as SearchBar } from "./ui/SearchBar";
